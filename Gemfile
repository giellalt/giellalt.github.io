source "https://rubygems.org"

# TESTING: pinned to the theme's gemify-theme-deps branch until that PR
# merges to main, then this should go back to `branch: "main"`.
group :jekyll_plugins do
  gem "jekyll-theme-giellalt", github: "giellalt/jekyll-theme-giellalt", branch: "gemify-theme-deps"
end

gem "octokit"      # fetch_github_repos.rb requires it directly
