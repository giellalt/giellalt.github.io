remote_theme: giellalt/jekyll-theme-giellalt@sitemap-whitespace

# Org pages site — served at the domain root. Set explicitly so asset URLs
# (relative_url) and SEO canonicals don't depend on jekyll-github-metadata's
# guess (which, without an API token, resolves them wrongly under Jekyll 4).
url: "https://giellalt.github.io"
baseurl: ""

title: GiellaLT

description: <a href="/AboutGiellaLT.html">GiellaLT</a> provides an infrastructure for rule-based language technology aimed at minority and indigenous languages, and streamlines building anything from keyboards to speech technology. Read <a href="https://indigenous-langtech.uit.no">more about why</a>, and see also our <a href="/Personvern.html">privacy document</a>.


plugins:
  - jekyll-default-layout
  - jekyll-gist
  - jekyll-github-metadata
  - jekyll-include-cache
  - jekyll-minifier
  - jekyll-optional-front-matter
  - jekyll-readme-index
  - jekyll-redirect-from
  - jekyll-relative-links
  - jekyll-remote-theme
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-titles-from-headings
  - jemoji

# Minification (production builds only — the workflow sets JEKYLL_ENV).
# HTML + JS: jekyll-minifier. CSS: sass below (jekyll-minifier's cssminify2
# corrupts var(--x) inside calc()).
sass:
  style: compressed
  sourcemap: never
jekyll-minifier:
  compress_css: false
  exclude:
    - '*typosreport*'

markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
  auto_ids: true
  footnote_nr: 1
  entity_output: as_char
  toc_levels: 1..6
  smart_quotes: lsquo,rsquo,ldquo,rdquo
  enable_coderay: false
  syntax_highlighter_opts:
    disable: true

# GitHub Pages kompatibilitet
repository: giellalt/giellalt.github.io
source_dir: ""

# Exclude problematic files from Jekyll processing
exclude:
  - "infra/test-slidev.sh"
  - "docs-prebuild.sh"
  - "fetch_github_repos.rb"
  - "package.json"
  - "package-lock.json"
  - "node_modules/"
  - "vendor/"
  - "Gemfile"
  - "Gemfile.lock"
  - "README.md"
  - "assets/js/README.md"
  - ".git/"
  - ".github/"
  - ".gitignore"
  - ".sass-cache/"
  - ".jekyll-cache/"
  - ".bundle/"
  - "*-slidev/"
  - "vendor/bundle/"
  - "vendor/cache/"
  - "vendor/gems/"
  - "vendor/ruby/"

# Keep raw Slidev files accessible
keep_files:
  - "*-slidev"
